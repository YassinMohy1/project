# دليل ربط Stripe بالموقع

## الخطوات بالعربي:

### 1️⃣ إنشاء حساب Stripe

1. روح على https://dashboard.stripe.com/register
2. سجل حساب جديد
3. أكد الإيميل بتاعك

### 2️⃣ الحصول على API Keys

1. بعد ما تسجل دخول، روح على: https://dashboard.stripe.com/apikeys
2. هتلاقي نوعين من الـ Keys:
   - **Publishable key** - بيبدأ بـ `pk_test_...` (للـ frontend)
   - **Secret key** - بيبدأ بـ `sk_test_...` (للـ backend) - اضغط "Reveal test key"

### 3️⃣ إضافة الـ Keys للـ Environment Variables

افتح ملف `.env` في المشروع وحط الـ Keys:

```env
# Stripe API Keys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51xxxxx  # حط هنا الـ Publishable key
STRIPE_SECRET_KEY=sk_test_51xxxxx             # حط هنا الـ Secret key
```

⚠️ **مهم جداً:**
- الـ `VITE_STRIPE_PUBLISHABLE_KEY` بيبدأ بـ `VITE_` عشان يشتغل في React
- الـ `STRIPE_SECRET_KEY` بدون `VITE_` عشان يتحط تلقائياً في الـ Edge Function

### 4️⃣ تفعيل Test Mode

في لوحة تحكم Stripe:
- تأكد إن التبديل في أعلى اليمين مكتوب عليه "Test mode"
- ده يخليك تجرب الدفع بدون فلوس حقيقية

### 5️⃣ كروت تجريبية للاختبار

استخدم الكروت دي للتجربة:

**كارت ناجح:**
- رقم الكارت: `4242 4242 4242 4242`
- تاريخ الانتهاء: أي تاريخ في المستقبل (مثلاً: 12/25)
- CVC: أي 3 أرقام (مثلاً: 123)
- ZIP: أي 5 أرقام (مثلاً: 12345)

**كارت فاشل (لاختبار الأخطاء):**
- رقم الكارت: `4000 0000 0000 0002`

**كارت يطلب تأكيد 3D Secure:**
- رقم الكارت: `4000 0025 0000 3155`

### 6️⃣ التحويل للـ Live Mode (الوضع الحقيقي)

لما تكون جاهز للإطلاق الحقيقي:

1. روح على https://dashboard.stripe.com/account/onboarding
2. أكمل معلومات الشركة/البيزنس
3. ارجع لـ https://dashboard.stripe.com/apikeys
4. غير التبديل من "Test mode" لـ "Live mode"
5. انسخ الـ Keys الجديدة (بتبدأ بـ `pk_live_` و `sk_live_`)
6. غير الـ Keys في ملف `.env`

```env
# Stripe Live Keys
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_51xxxxx
STRIPE_SECRET_KEY=sk_live_51xxxxx
```

### 7️⃣ اختبار الدفع

1. شغل الموقع: `npm run dev`
2. روح على صفحة Checkout: http://localhost:5173/checkout
3. املا البيانات
4. استخدم كارت تجريبي من اللي فوق
5. اضغط "Proceed with Payment"

### 8️⃣ متابعة المدفوعات

- روح على https://dashboard.stripe.com/payments
- هتلاقي كل المدفوعات (التجريبية والحقيقية)
- تقدر تشوف تفاصيل كل دفعة

---

## 📌 ملاحظات مهمة:

1. **الأمان**:
   - لا تنشر الـ Secret Key في الكود أبداً
   - خليه في ملف `.env` بس
   - متضيفش ملف `.env` على Git

2. **الـ Edge Function**:
   - الـ Edge Function بيستخدم الـ `STRIPE_SECRET_KEY` تلقائياً
   - مش محتاج تعمل أي تعديل في الكود

3. **الـ Frontend**:
   - الكود بيستخدم `VITE_STRIPE_PUBLISHABLE_KEY` تلقائياً
   - موجود في `src/App.tsx`

---

## ❓ مشاكل شائعة:

### المشكلة: "Stripe is not configured"
**الحل**: تأكد إنك حطيت الـ Keys الصح في ملف `.env`

### المشكلة: "Invalid API key"
**الحل**: تأكد إنك نسخت الـ Key كامل بدون مسافات زيادة

### المشكلة: "Payment failed"
**الحل**: استخدم واحد من الكروت التجريبية اللي فوق

---

## 📞 دعم فني:

- Stripe Docs: https://stripe.com/docs
- رقم الدعم: 888-602-6667 (موجود في الموقع)
